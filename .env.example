# Admin password for accessing the admin panel
# Change this to a secure password before deployment
VITE_ADMIN_PASSWORD=admin123

# Port for the application (default: 5000)
# Replit will set this automatically
PORT=5000
